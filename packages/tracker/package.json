{
  "name": "@onemetric/tracker",
  "version": "0.0.0",
  "private": true,
  "description": "OneMetric embeddable analytics tracking script.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --format=iife --target=es2017 --outfile=../../apps/web/public/onemetric.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5"
  }
}
